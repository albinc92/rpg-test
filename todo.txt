- natural camera sway in-game just like the in-battle camera does (it should be possible to issue camera shake effects through in-game scripts like through the visual programming we use for programming NPCs...)
- generate a registry of spirits. the idea is to create a balanced system that tries to capture every dual-type of our existing elements and single types... i.e. one family which is only water based, one which is mixed water/grass etc... and stat lines which are unique and balanced given some metric-based foundation in order to keep things balanced... i.e. if a spirit is heavy on the speed attribute (-> higher damage throughput etc... then another stat should suffer as consequence or something....) thoughts?