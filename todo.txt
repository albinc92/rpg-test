BATTLE SYSTEM TODOS
====================

[x] 1. Status effect tick damage (burn/poison per turn) + duration countdown
      - BattleSystem.js processStatusEffects() runs at start of each turn
      - Burn: 8% maxHP, Poison: 6% maxHP, Paralyze: 25% skip, Freeze/Sleep: full skip
      - Durations decrement, expired effects removed with log message

[x] 2. Battle loot drops — connect LootSystem to battle rewards
      - On enemy KO, rolls loot via game.lootSystem.generateLoot()
      - Dropped items added to rewards.items[] with name for display
      - Items added to inventory on victory in exitBattle()

[x] 3. Multi-target ability indicator during target_select
      - Arrows drawn on ALL targets when ability is all_enemies/all_allies
      - "ALL" label shown below arrows

[x] 4. Item action text — show item name as floating action text
      - Added 'item' case to queuePlayerAction action text dispatch