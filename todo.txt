BATTLE SYSTEM TODOS
====================

[ ] 1. Status effect tick damage (burn/poison per turn) + duration countdown
      - BattleSystem.js update() needs to process statusEffects each ATB cycle
      - Decrement duration, apply burn/poison damage, remove expired effects

[ ] 2. Battle loot drops — connect LootSystem to battle rewards
      - On enemy KO, roll loot via game.lootSystem.generateLoot()
      - Add dropped items to rewards.items[]
      - Add items to inventory on victory (exitBattle)

[ ] 3. Multi-target ability indicator during target_select
      - Show "ALL" text or highlight all targets when ability is all_enemies/all_allies
      - renderTargetCursorOnSprite should draw arrows on all targets

[ ] 4. Item action animation — items should trigger user movement to target
      - BattleSystem action animation only moves for 'attack', items skip animation
      - Add 'item' to animated action types so user walks to target and back