- 
- generate a "placeholder" registry of spirits. the idea is to create a balanced system that tries to capture every single element type and dual element types... i.e. one family which is only water based, one which is mixed water/grass etc... and stat lines which are unique and balanced given some metric-based foundation in order to keep things balanced... i.e. if a spirit is heavy on the speed attribute (-> higher damage throughput etc... then another stat should suffer as consequence or something....) thoughts? maybe we could have some sort of elemental influence on stats... like... fire element are naturally high on offense, lightning types are naturally speedy... or maybe thats too predictive and boring... i don't know.. but its a cool idea anyhow. lets explore it together.
- a test NPC that has a script which tests ALL our custom script language features.
  - revisit available script support for NPC and see if theres something we might need... like making other npcs on screen talk... or whatever. make the npc itself or other npcs walk. basically so we can program cutscenes. more stuff like that...
- inventory system.
- world map zone levels that dictates the level of the spirits that spawn in them
  and the levelled item lists that can drop from chests
- lootable chests.
  - dynamic loot system from chests with levelled lists based on zone level (see feature above)
- dynamic spawn system based on biome and zone level which determines which types of spirits that can spawn in a given map? thoughts?

================================================================================
PLANNED FEATURES
================================================================================

────────────────────────────────────────────────────────────────────────────────
1. BIOME-DRIVEN ENVIRONMENTAL PARTICLES
────────────────────────────────────────────────────────────────────────────────
WHAT WE HAVE:
  - WeatherSystem renders 3 particle types: rain (streaks), snow (circles), leaves (ellipses)
  - Leaves have 5 color variants (green, orange, red, brown, sakura) but are set per-map
    via the map "particles" property — NOT driven by biome
  - BiomeWeatherSystem knows the biome for every cell but only outputs weather channels
    (rain/snow/wind/fog/cloud) — no biome particle hints
  - Fog is rendered as a flat screen-space overlay with wispy gradient animation

WHAT TO ADD:
  [x] Biome ambient particle table in BiomeEffectsSystem:
      - woodland / dense-forest → falling leaves (green/brown), fireflies at night
      - grassland / plains / meadow → pollen/dandelion seeds, fireflies at dusk/night, dragonflies
      - desert / arid-desert → dust motes, sand wisps (wind-driven)
      - snow / tundra → ice crystals / diamond dust (subtle, separate from snow weather)
      - mountain / high-mountain → dust motes
      - lake / river-valley → mist wisps, dragonflies at dawn/dusk
  [x] New particle types in BiomeEffectsSystem:
      - Fireflies: glowing dots with random fade-in/fade-out, slow drift, night-only
      - Dust/sand: small tan/brown dots, wind-driven direction, faster in sandstorms
      - Pollen/seeds: tiny white/yellow floaters, gentle random drift
      - Mist wisps: semi-transparent cloud puffs near water, slow horizontal movement
      - Ice crystals: tiny sparkling points, subtle shimmer effect
      - Dragonflies: iridescent darting insects, hover+dash, dawn/dusk only (NEW)
      - Sand wisps: spiraling wind-driven tendrils, elongated rendering (NEW)
      - Falling leaves: swaying rotating ellipses, gravity drift (NEW)
  [x] BiomeEffectsSystem handles biome particles independently (reads biome from cellBiome)
  [x] Time-of-day gating (fireflies night, dragonflies dawn/dusk, pollen day)
      - Fixed dnc.time → dnc.timeOfDay bug that broke all time gating

────────────────────────────────────────────────────────────────────────────────
2. MINIMAP
────────────────────────────────────────────────────────────────────────────────
WHAT WE HAVE:
  - 30×30 world grid with biome + region data per cell
  - World map viewer (M key) that shows the full grid
  - Each map has gridX/gridY coordinates
  - Player position tracked in real-time

WHAT TO ADD:
  [x] MinimapSystem.js — renders a small corner overlay (bottom-right or top-right)
  [x] Shows nearby cells from the world grid (e.g. 5×5 or 7×7 around player)
  [x] Color-coded by biome (reuse world map colors)
  [x] Blinking player dot at current position within the cell
  [x] Fog of war — only show visited cells
  [x] Toggle with a key (e.g. Tab or N)
  [x] Optional: show NPC/portal/chest dots on current map
  [x] Optional: compass rose or cardinal direction labels
  [x] Semi-transparent background so it doesn't obscure gameplay
  [x] Hide during battles, menus, cutscenes

────────────────────────────────────────────────────────────────────────────────
3. DYNAMIC BGM SYSTEM
────────────────────────────────────────────────────────────────────────────────
WHAT WE HAVE:
  - AudioManager has full crossfade infrastructure (5s default, 50-step ramp)
  - playBGM(filename, crossfadeDuration) with dedup + race-condition handling
  - BGM is set per-map externally (GameEngine calls playBGM on map load)
  - All maps currently use the same track (01.mp3)
  - BiomeWeatherSystem already maps biomes → ambience audio files
  - 5 audio channels: BGM, ambience, weather, wind, effects

WHAT TO ADD:
  [x] BiomeBGMSystem.js — selects BGM track based on:
      - Current biome (primary driver) ✓
      - Time of day (night variants) ✓
      - Weather intensity (stormy variants) — future enhancement
      - Battle proximity (tension layer) — future enhancement
  [x] Biome → BGM mapping table:
      - woodland / dense-forest → bgm-forest-{day|night}.mp3
      - grassland / plains / meadow → bgm-field-{day|night}.mp3
      - desert / arid-desert → bgm-desert-{day|night}.mp3
      - snow / tundra → bgm-snow-{day|night}.mp3
      - mountain / high-mountain → bgm-mountain-{day|night}.mp3
      - lake / river-valley → bgm-water-{day|night}.mp3
  [x] Time-of-day variants (day/night tracks per biome group)
  [x] Crossfade on biome change (6s) and day↔night (8s) via AudioManager
  [x] Don't interrupt during battle (disabled on BattleState.enter, re-enabled on exit)
  [x] Region continuity — same biome = same track, no restart on map transition
  [x] Placeholder: 12 silent MP3 placeholders created, swap in real music later

────────────────────────────────────────────────────────────────────────────────
4. FOOTPRINT / TRAIL SYSTEM
────────────────────────────────────────────────────────────────────────────────
WHAT WE HAVE:
  - Player.js has footstep audio system (timer-based, speed-aware, running support)
  - Biome data available per cell
  - Tile-based movement with smooth interpolation

WHAT TO ADD:
  [ ] FootprintSystem.js — visual footprints behind the player
  [ ] Spawns a footprint sprite/shape at player position every N pixels of movement
  [ ] Biome-aware appearance:
      - Sand (desert): visible sandy footprints, slow fade (~10s)
      - Snow: deep prints, longest persistence (~15s)
      - Grass/plains: subtle darker patches, fast fade (~5s)
      - Stone/mountain: no prints (too hard)
      - Water/lake: splash rings, very fast fade (~2s)
  [ ] Direction-aware (left/right foot alternation based on facing)
  [ ] Alpha fade-out over lifetime
  [ ] Max pool size (e.g. 50 prints) — oldest get recycled
  [ ] Render below player but above ground layer
  [ ] NPC footprints too? (optional, performance consideration)

────────────────────────────────────────────────────────────────────────────────
5. BIOME POST-PROCESS SHADER EFFECTS
────────────────────────────────────────────────────────────────────────────────
WHAT WE HAVE:
  - WebGL post-process shader with these uniforms ALREADY WORKING:
      ✓ u_brightness (0.2–1.0) — day/night + weather darkening
      ✓ u_saturation (0.6–1.0) — desaturation from weather/night
      ✓ u_temperature (-0.5 to +0.6) — warm/cool color shift
      ✓ u_darknessColor vec3 — night tint color
      ✓ u_vignetteIntensity (default 0.5) — edge darkening
      ✓ u_aberrationIntensity (default 0.5) — chromatic aberration
      ✓ u_sharpenIntensity (user setting) — unsharp mask
      ✓ u_bloomIntensity (user setting) — soft glow on brights
      ✓ u_lightMask — point light mask texture
      ✓ FXAA anti-aliasing (separate pass)
  - RenderSystem already applies shader params per-frame
  - DayNightCycle drives time-based params
  - WeatherSystem drives weather-based darkening/desaturation

WHAT TO ADD — new shader uniforms + biome profiles:
  [x] Heat haze / distortion (desert biomes):
      - New uniform: u_hazeIntensity, u_hazeSpeed
      - UV displacement using sin wave, subtle ripple near ground
      - Intensity driven by biome + time of day (stronger at midday)
  [x] Underwater tint (water/lake biomes or submerged areas):
      - New uniform: u_underwaterTint vec3 (blue-green)
      - Slightly increased bloom + reduced saturation
      - Optional: wavy UV distortion (gentle)
  [x] Cold desaturation (snow/tundra biomes):
      - Slight blue tint via u_temperature
      - Lower saturation baseline
      - Higher vignette
  [x] Warm tint (desert biomes):
      - Slight warm temperature shift
      - Slightly increased brightness
  [x] Forest mood (woodland biomes):
      - Slightly green-tinted darknessColor
      - Standard vignette
      - Subtle bloom increase (dappled light feel)
  [x] Biome shader profile table:
      - Each biome maps to { temperature, saturation, vignette, bloom,
        hazeIntensity, underwaterTint, etc. }
      - Smoothly interpolate between profiles on biome change
      - Layer ON TOP of day/night + weather (don't replace, combine)
  [ ] Settings menu controls for new effects (on/off toggles)

================================================================================